import React from 'react'

const ServiceCard = ({ id, name, description, responseTime, availability, tehnicianAssignment, rating, price }) => {
    return (
        <div>
            {name}
        </div>
    )
}

export default ServiceCard
